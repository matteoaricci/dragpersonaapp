<!DOCTYPE html>
<html>
  <head>
    <title>Drag Yourself</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand logo_text" href="/home">
        <%= image_tag("drag_yourself_logo.png", class:"d-inline-block align-top logo", alt:"logo") %>
        Drag Yourself
      </a>
      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item">
            <a class="nav-link" href="/users">Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/houses">Houses</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/talents">Talents</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cities">Cities</a>
          </li>
        </ul>
      </div>
      <div>
        <% if controller.controller_name != "sessions" %>
          <% if session[:user_id] %>
            <div>
              <h4 class="logged_in_text center"><%= link_to User.find(session[:user_id]).drag_name, user_path(current_user) %></h4>
            </div>
          <% else %>
            <div>
              <h4 class="logged_in_text"><%= link_to "Login", login_path %></h4> 
            </div>
          <% end %>
        <% end %>
      
        <% if flash[:login_warning] %>
          <%= flash[:login_warning] %>
        <% end %>
      </div>
    </nav>

    <%= yield %>
    
  </body>
</html>
